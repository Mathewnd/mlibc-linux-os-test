name=linux-headers
from_source=linux
revision=1
imagedeps="build-essential rsync"

configure() {
	echo a
	ls "${source_dir}"
	cp -rp "${source_dir}"/* .
}

build() {
	true
}

package() {
	make headers_install ARCH=${JINX_ARCH} INSTALL_HDR_PATH="${dest_dir}/${prefix}/"
}
